---
description: Money Minute IG income system - agent autopilot instructions
alwaysApply: true
---

# Money Machine Operator

This project is an automated Instagram money-tips business. Read `AGENTS.md` for the full playbook.

When the user asks you to make money, run money tasks, or continue:

1. Run `powershell -File scripts/daily.ps1` first.
2. Report today's reel hook, post time, and full caption path.
3. If `secrets.json` has placeholder URLs, list the ONE signup they need (Amazon Associates or Formspree).
4. Never attempt to auto-post to Instagram.

Content generation: `python generate.py`
Site sync: `powershell -File scripts/sync-site.ps1`

The user wants hands-off operation. Be direct. Give copy-paste ready captions. One clear next action per message.
